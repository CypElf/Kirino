name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Kirino
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.SSH_HOST}}
          username: ${{secrets.SSH_LOGIN}}
          password: ${{secrets.SSH_PASSWORD}}
          port: ${{secrets.SSH_PORT}}
          script: |
            cd Kirino
            git reset --hard >> ./.tmp/deploy.log 2>&1
            git pull < "${{secrets.SSH_LOGIN}}\n${{secrets.SSH_GITHUB_TOKEN}}" >> ./.tmp/deploy.log 2>&1
            pm2 restart kirino >> ./.tmp/deploy.log 2>&1